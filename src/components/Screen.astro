---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div
  class={`w-full px-6 md:px-12 lg:px-24 xl:px-32 mx-auto max-w-screen-2xl ${className}`}
>
  <slot />
</div>
